// Generated by panelkit from tools/panels/PatchAudit.py. Do not edit; edit the spec.
//
// Everything the panel's C++ needs to agree with res/PatchAudit.svg: the millimetre
// grid, the widget positions and the whole silkscreen. Rack's nanosvg discards
// <text>, so labels cannot live in the SVG -- they are drawn at runtime from
// the table below, generated from the same spec that drew the artwork.
//
// Every declaration here is `static`, i.e. private to the translation unit, so
// the three panels in this plugin cannot collide. That also means no
// translation unit may include two of these.
#pragma once
#include "../PanelTheme.hpp"

namespace panel {

// --- identity --------------------------------------------------------------
static const int   HP = 26;
static const float W  = 132.0800f;  // mm
static const float H  = 128.5000f;  // mm

// --- panel metrics, mirrored from tools/panels/PatchAudit.py ---
static const float BAND_FOOTER    = 114.6000f;
static const float BTN_H          = 7.8000f;
static const float BTN_W          = 59.8400f;
static const float BTN_Y          = 115.4000f;
static const float CHOICE_H       = 7.4000f;
static const float CHOICE_W       = 58.8400f;
static const float CHOICE_Y       = 25.0000f;
static const float IW             = 121.6800f;
static const float LIST_H         = 70.4000f;
static const float LIST_INSET     = 0.8000f;
static const float LIST_Y         = 35.6000f;
static const float M              = 5.2000f;
static const float NAV_W          = 22.0000f;
static const float PAGE_H         = 6.5000f;
static const float PAGE_Y         = 107.2000f;
static const float PROG_H         = 1.8000f;
static const float PROG_W         = 110.0800f;
static const float PROG_X         = 11.0000f;
static const float PROG_Y         = 124.4000f;
static const float QUERY_Y0       = 10.0000f;
static const float QUERY_Y1       = 34.0000f;
static const int   ROWS_VISIBLE   = 8;
static const float ROW_H          = 8.6000f;
static const float SEARCH_H       = 8.0000f;
static const float SEARCH_Y       = 15.6000f;
static const float STATUS_Y       = 13.6000f;

// --- silkscreen ------------------------------------------------------------
static const Label LABELS[] = {
	{ 11.5600f,   5.5000f, 10.50f, 1.50f, PAPER    , NVG_ALIGN_LEFT | NVG_ALIGN_BASELINE, false, "PATCHAUDIT"},
	{120.5200f,   5.5000f,  6.00f, 0.00f, SAGE     , NVG_ALIGN_RIGHT | NVG_ALIGN_BASELINE, false, "PATCHSTORAGE RETURNS"},
	{ 11.5600f,   8.2500f,  7.40f, 0.00f, LIME     , NVG_ALIGN_LEFT | NVG_ALIGN_BASELINE, true,  "$"},
	{ 13.7637f,   8.0000f,  4.60f, 0.40f, SAGE     , NVG_ALIGN_LEFT | NVG_ALIGN_BASELINE, false, "MOON TECHNOLOGIES"},
	{127.8800f,   8.0000f,  4.60f, 0.40f, SAGE     , NVG_ALIGN_RIGHT | NVG_ALIGN_BASELINE, false, "FORM 4564"},
};

/** Draws this panel's silkscreen. `static` so each module keeps its own,
    bound to its own table. */
static inline void addLabels(app::ModuleWidget* mw) {
	addLabels(mw, LABELS, sizeof(LABELS) / sizeof(LABELS[0]));
}

// --- widget positions, by the names used in tools/panels/PatchAudit.py -----
// Positions are mm; feed them to mm() or createParamCentered.

} // namespace panel
